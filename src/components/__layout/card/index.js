import React from "react"

const BirthdayCard = () => {
  return (
    <Card width={256}>
      <Image src={props.image} />
      <Heading>Card</Heading>
    </Card>
  )
}

export default BirthdayCard
